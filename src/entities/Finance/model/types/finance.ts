export interface Finance {
    symbol: string;
    name: string;
    currency: string;
    stockExchange: string;
    exchangeShortName: string;
}

export interface Company {
    ticker: string;
    name: string;
}
